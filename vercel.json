{
    "build": {
      "env": {
        "CI": "true"
      },
      "command": [
        "if [ -z \"$SMTP_HOST\" ]; then echo \"SMTP_HOST is not set\"; exit 1; fi",
        "if [ -z \"$SMTP_PORT\" ]; then echo \"SMTP_PORT is not set\"; exit 1; fi",
        "if [ -z \"$SMTP_USER\" ]; then echo \"SMTP_USER is not set\"; exit 1; fi",
        "if [ -z \"$SMTP_PASSWORD\" ]; then echo \"SMTP_PASSWORD is not set\"; exit 1; fi",
        "if [ -z \"$SMTP_FROM_NAME\" ]; then echo \"SMTP_FROM_NAME is not set\"; exit 1; fi",
        "if [ -z \"$SMTP_FROM_ADDRESS\" ]; then echo \"SMTP_FROM_ADDRESS is not set\"; exit 1; fi"
      ]
    }
  }